export interface Book {
    id: number;          
    title: string;
    author: string;
    pages: number;
    year: number;       
    imageUrl: string;  
    isbn: string;       
    publisher: string;  
  }